<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Basemap tiles and pack downloads need the network; installed packs then
         work fully offline. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Track recording is useless without these. Android stops delivering
         location to a backgrounded app, and a phone in a pocket with the screen
         off is a backgrounded app, so a recorded walk came out as straight lines
         between the moments the user happened to look at the screen. Geolocator
         declares the location service itself but not these permissions; without
         them the service refuses to start on Android 14 and up.

         WAKE_LOCK is what actually fixes it rather than merely allowing the
         service: geolocator's own documentation notes that without a wake lock
         "the system can still sleep and all location events will be received at
         once when the system wakes up again", which is the same wrong answer
         arriving later. POST_NOTIFICATIONS is for the notice the service has to
         show while it holds the GPS; somebody whose battery is draining in their
         pocket deserves to know what is doing it. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Backup stays on, but scoped. The default backs up the whole data
         directory, which here means the installed province packs and the offline
         basemap tile store: tens to hundreds of megabytes the user can
         re-download for free, against a 25 MB quota that abandons the backup
         rather than trimming it. The two files below say what is left out and
         why. fullBackupContent is read below Android 12, dataExtractionRules
         from 12 up, and they have to agree. -->
    <application
        android:label="OpenWoodsMap"
        android:name="${applicationName}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Resolve a browser for the official policy report links. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
